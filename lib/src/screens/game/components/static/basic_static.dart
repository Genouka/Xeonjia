import 'package:xeonjia/src/screens/game/components/abstract_basic.dart';

// Basic static component (it is just BasicComponent)
class BasicStaticComponent extends BasicComponent {
  BasicStaticComponent(tile) : super.fromTile(tile);
}
